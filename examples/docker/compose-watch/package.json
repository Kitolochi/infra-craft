{
  "name": "compose-watch-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Docker Compose Watch — sync, rebuild, and sync+restart modes",
  "scripts": {
    "dev": "docker compose watch",
    "up": "docker compose up --build -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
